variant: fiot
version: 1.0.0
storage:
  files:
    - path: /etc/motd
      mode: 0644
      contents:
        inline: |
          ${message}
passwd:
  users:
    - name: core
      ssh_authorized_keys:
        - ssh-key foo

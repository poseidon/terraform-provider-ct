variant: flatcar
version: 1.1.0
storage:
  files:
    - path: "/etc/coreos/update.conf"
      mode: 0644
      contents:
        inline: |
          GROUP="alpha"
          SERVER="update.example.com/v1/update"
